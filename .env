# Directory where agent repositories are cloned
DOWNLOADS_DIR=downloads

# Path to the agents registry file
REGISTRY_PATH=registry/agents.json

# Path to the Dockerfile template
DOCKERFILE_TEMPLATE=templates/Dockerfile.template

# Temporary directory for per-run input files
TMP_DIR=tmp
